#include <stdio.h>
#include <string.h>
int main()
{
    char str[1000];
    int test;
    scanf("%d",&test);
    while(test--)
    {
        scanf("%s",str);
        int i, sum1=0, sum2=0;
        for(i=0; i<strlen(str); i++)
        {

        }
        printf("%d\n",t1);
    }
    return 0;
}
