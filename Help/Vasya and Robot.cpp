#include <stdio.h>
#include <iostream>
using namespace std;
int main()
{
    int n, l , r , q1, q2;
    while(scanf("%d %d %d %d %d",&n,&l,&r,&q1,&q2)==5)
    {
        int weight[n];
        for(int i=0;i<n;i++)
            scanf("%d",&weight[i]);
        int count=1, i=0 , flag=0, j=n-1, extra=0;
        while(count<n)
        {

            if(extra+)
        }
    }
}
